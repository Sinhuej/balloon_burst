import '../core/game_manager.dart';

class PowerupManager {
  final GameManager gameManager;

  PowerupManager(this.gameManager);

  void applyDoubleScore(Duration duration) {}
  void applyShield(Duration duration) {}
}
