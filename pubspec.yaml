name: balloon_burst
description: "Balloon Burst - A TapJunkie Mini-Game"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"
  flutter: ">=3.13.0"

dependencies:
  tapjunkie_engine:

    git:

      url: https://github.com/${GITHUB_ACTOR}:${GITHUB_TOKEN}@github.com/Sinhuej/tapjunkie_engine.git

      url: "https://${GITHUB_ACTOR}:${GITHUB_TOKEN}@github.com/Sinhuej/tapjunkie_engine.git"
  flutter:
    sdk: flutter
  flame: ^1.17.0
    url: https://github.com/${GITHUB_ACTOR}:${GITHUB_TOKEN}@github.com/Sinhuej/tapjunkie_engine.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sprites/
    - assets/images/
